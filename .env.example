ECS_API_URL=https://

# express (be-push-notification-tool). Public on purpose: its CORS allows this app's origin
# directly, so the browser calls it straight, no proxy hop -- unlike ECS_API_URL, this is not a secret.
NEXT_PUBLIC_EXPRESS_API_URL=http://localhost:8080
